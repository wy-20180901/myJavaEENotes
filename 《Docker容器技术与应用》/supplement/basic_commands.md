[toc]

# 常见命令

```shell
docker search mysql
docker version
docker images
docker ps 
docker container ls
docker image prune #清理临时的、没有被使用的镜像文件
docker pull mysql
docker image ls
docker rmi mysql 
docker image rm mysql
```

